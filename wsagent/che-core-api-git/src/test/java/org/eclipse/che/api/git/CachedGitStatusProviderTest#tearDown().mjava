  @AfterClass
  public void tearDown() throws Exception {
    Files.walk(projectPath.getParent())
        .sorted(Comparator.reverseOrder())
        .map(Path::toFile)
        .forEach(File::delete);
  }

