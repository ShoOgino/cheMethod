  public Supplier<Integer> source(String input) {
    return new Supplier<Integer>() {
      private int pos = 0;

      @Override
      public Integer get() {
        if (pos >= input.length()) {
          return -1;
        } else {
          return (int) input.charAt(pos++);
        }
      }
    };
  }

