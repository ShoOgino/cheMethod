    /** @see Status#getUntracked() */
    @Override
    public List<String> getUntracked() {
        if (untracked == null) {
            untracked = new ArrayList<>();
        }
        return untracked;
    }

