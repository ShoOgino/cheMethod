    @Override
    public String toString() {
      return new StringBuilder()
          .append(failures)
          .append(" failures and ")
          .append(errors)
          .append(" errors")
          .toString();
    }

