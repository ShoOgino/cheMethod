  /**
   * Creates new VirtualFile which denotes folder and use this one as parent folder.
   *
   * @param name name. If name is string separated by '/' all nonexistent parent folders must be
   *     created.
   * @return newly create VirtualFile that denotes folder
   * @throws ForbiddenException if copy operation is forbidden
   * @throws ConflictException if item with specified {@code name} already exists
   * @throws ServerException if other error occurs
   */
  public FolderEntry createFolder(String name)
      throws ConflictException, ServerException, ForbiddenException {
    return new FolderEntry(getVirtualFile().createFolder(name), projectRegistry);
  }

