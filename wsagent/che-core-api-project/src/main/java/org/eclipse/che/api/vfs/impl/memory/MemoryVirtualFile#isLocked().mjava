  @Override
  public boolean isLocked() {
    checkExistence();
    final LockHolder myLock = lock;
    if (myLock != null) {
      if (isExpired(myLock)) {
        lock = null;
        return false;
      }
      return true;
    }
    return false;
  }

