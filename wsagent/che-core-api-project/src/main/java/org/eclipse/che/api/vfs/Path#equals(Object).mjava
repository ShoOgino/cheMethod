  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o instanceof Path) {
      Path path = (Path) o;
      return Arrays.equals(elements, path.elements);
    }
    return false;
  }

