  @Inject
  public IndexedFileUpdateConsumer(
      @Named("che.user.workspaces.storage") File root, VirtualFileSystemProvider vfsProvider) {
    this.root = root;
    this.vfsProvider = vfsProvider;
  }

