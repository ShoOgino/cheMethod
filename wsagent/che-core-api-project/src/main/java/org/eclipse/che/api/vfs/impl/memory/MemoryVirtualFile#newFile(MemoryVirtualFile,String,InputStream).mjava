  static MemoryVirtualFile newFile(MemoryVirtualFile parent, String name, InputStream content)
      throws IOException {
    return new MemoryVirtualFile(
        parent, name, content == null ? new byte[0] : ByteStreams.toByteArray(content));
  }

