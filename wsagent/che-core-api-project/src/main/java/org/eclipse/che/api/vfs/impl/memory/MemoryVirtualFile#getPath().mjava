    @Override
    public Path getPath() {
        checkExistence();
        MemoryVirtualFile parent = this.parent;
        if (parent == null) {
            return Path.ROOT;
        }
        Path parentPath = parent.getPath();
        return parentPath.newPath(getName());
    }

