  public void broadcastLowDiskSpaceMessage() {
    clientSubscriptionHandler
        .getEndpointIds()
        .forEach(
            it ->
                transmitter
                    .newRequest()
                    .endpointId(it)
                    .methodName(JSON_RPC_METHOD_NAME)
                    .noParams()
                    .sendAndSkipResult());
  }

