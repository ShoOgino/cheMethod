    @PreDestroy
    public void stop() {
        try {
            launcher.shutdown();
        } catch (InterruptedException e) {
            LOG.error("Can't properly stop thread pull launcher: ", e);
        }
    }

