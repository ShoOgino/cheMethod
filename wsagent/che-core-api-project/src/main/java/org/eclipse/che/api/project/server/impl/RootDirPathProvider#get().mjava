  @Override
  public String get() {

    String path = System.getenv("CHE_PROJECTS_ROOT");
    if (path == null && rootFile != null) {
      path = rootFile.getPath();
    }
    if (path == null) {
      path = DEF;
    }

    return path;
  }

