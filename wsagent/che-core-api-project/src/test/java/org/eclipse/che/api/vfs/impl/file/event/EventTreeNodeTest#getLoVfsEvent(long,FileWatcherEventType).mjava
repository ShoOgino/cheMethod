    private LoEvent getLoVfsEvent(long time, FileWatcherEventType type) {
        return LoEvent.newInstance()
                      .withPath(TEST_PATH)
                      .withTime(time)
                      .withItemType(FILE)
                      .withEventType(type);
    }

