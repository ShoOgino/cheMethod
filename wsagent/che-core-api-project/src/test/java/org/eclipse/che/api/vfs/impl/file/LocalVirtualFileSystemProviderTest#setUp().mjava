  @Before
  public void setUp() throws Exception {
    File targetDir =
        new File(Thread.currentThread().getContextClassLoader().getResource(".").getPath())
            .getParentFile();
    fsRootDirectory = new File(targetDir, NameGenerator.generate("index-root", 4));
    assertTrue(fsRootDirectory.mkdir());
    fileSystemProvider =
        new LocalVirtualFileSystemProvider(fsRootDirectory, mock(SearcherProvider.class));
  }

