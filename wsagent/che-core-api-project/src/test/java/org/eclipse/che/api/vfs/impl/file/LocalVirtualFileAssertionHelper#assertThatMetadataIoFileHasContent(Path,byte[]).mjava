  public void assertThatMetadataIoFileHasContent(Path virtualFilePath, byte[] bytes)
      throws IOException {
    assertArrayEquals(bytes, Files.toByteArray(getMetadataIoFile(virtualFilePath)));
  }

