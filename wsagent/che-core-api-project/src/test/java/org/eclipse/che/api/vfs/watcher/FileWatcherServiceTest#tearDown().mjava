  @AfterClass
  public void tearDown() throws Exception {
    service.stop();

    reset(handler);

    for (int i = 0; i < 10; i++) {
      if (service.isStopped()) {
        return;
      }
      Thread.sleep(1_000);
    }
    fail();
  }

