    /**
     * `step` command.
     */
    public GdbInfoLine step() throws IOException, InterruptedException, GdbParseException {
        sendCommand("step");
        outputs.take();
        return infoLine();
    }

