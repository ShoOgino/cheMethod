    /**
     * `quit` command.
     */
    public void quit() throws IOException, GdbException, InterruptedException {
        try {
            sendCommand("quit", false);
        } finally {
            stop();
        }
    }

