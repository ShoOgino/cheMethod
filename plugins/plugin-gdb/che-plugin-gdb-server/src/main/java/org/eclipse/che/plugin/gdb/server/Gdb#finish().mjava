    /**
     * `finish` command.
     */
    public GdbInfoLine finish() throws IOException, InterruptedException, GdbParseException {
        sendCommand("finish");
        outputs.take();
        return infoLine();
    }

