    @Override
    public void serialize(DataOutputStream out) throws IOException {
      super.serialize(out);
      if (content != null) {
        out.write(content.length);
        out.write(content);
      } else {
        out.writeInt(0);
      }
    }

