    protected synchronized T getOrCreateWrappedObject() throws RemoteException {
        if (wrapped == null) {
            wrapped = create();
            wrappedCreated();
        }

        return wrapped;
    }

