    @Override
    public void perform() {
        projectManager.resolveMavenProject(mavenProject.getProject(), mavenProject);
        if (afterTask != null) {
            afterTask.run();
        }

    }

