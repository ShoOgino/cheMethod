  @Override
  public void perform() {
    IProject project = mavenProject.getProject();
    if (!project.exists()) {
      return;
    }
    projectManager.resolveMavenProject(project, mavenProject);
    if (afterTask != null) {
      afterTask.run();
    }
  }

