    /**
     * Returns list of profiles.
     */
    public List<Profile> getProfiles() {
        if (profiles == null) {
            return emptyList();
        }
        return new ArrayList<>(profiles);
    }

