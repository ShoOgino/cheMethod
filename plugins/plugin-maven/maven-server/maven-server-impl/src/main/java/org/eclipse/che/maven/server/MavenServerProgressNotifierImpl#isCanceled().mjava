    @Override
    public boolean isCanceled() throws RemoteException {
        if(canceled){
            return true;
        }
        
        if(delegate.isCanceled()){
            canceled = true;
            return canceled;
        }
        
        return false;
    }

