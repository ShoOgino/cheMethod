        public File getFile(String type) {
            if (output == null || MavenConstants.POM_EXTENSION.equalsIgnoreCase(type)) {
                return file;
            } else {
                return output;
            }
        }

