    public MavenResource(String directory,
                         boolean filtered,
                         String targetPath,
                         List<String> includes,
                         List<String> excludes) {
        this.directory = directory;
        this.filtered = filtered;
        this.targetPath = targetPath;
        this.includes = includes;
        this.excludes = excludes;
    }

