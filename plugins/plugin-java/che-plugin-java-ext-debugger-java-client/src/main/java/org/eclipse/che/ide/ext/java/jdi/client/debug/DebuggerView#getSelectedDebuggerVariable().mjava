    /**
     * Returns selected variable in the variables list on debugger panel or null if no selection.
     *
     * @return selected variable or null if no selection.
     */
    DebuggerVariable getSelectedDebuggerVariable();

