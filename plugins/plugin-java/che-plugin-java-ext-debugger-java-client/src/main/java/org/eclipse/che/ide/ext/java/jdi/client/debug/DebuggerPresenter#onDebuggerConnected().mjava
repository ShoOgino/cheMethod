    private void onDebuggerConnected() {
        startCheckingEvents();
        eventBus.fireEvent(createConnectedStateEvent(DebuggerPresenter.this));

        showAndUpdateView();
        showDebuggerPanel();
    }

