    private void send(AntMessage message) {
        try {
            out.writeObject(message);
            if (autoFlush) {
                out.flush();
            }
        } catch (IOException e) {
            throw new IllegalStateException(e);
        }
    }

