        @Override
        protected void done() {
            super.done();
            endTime = System.currentTimeMillis();
            try {
                logger.close();
                LOG.debug("Close logger {}", logger);
            } catch (IOException e) {
                LOG.warn(e.getMessage(), e);
            }
        }

