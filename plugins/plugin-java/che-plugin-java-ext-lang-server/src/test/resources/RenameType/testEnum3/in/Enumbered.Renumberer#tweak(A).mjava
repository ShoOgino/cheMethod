        A tweak(A a) {
            switch (a) {
            case ONE:
                return TWO;
            case TWO:
                return A.THREE;

            default:
                throw new IllegalArgumentException(a.toString());
            }
        }

