        B tweak(B a) {
            switch (a) {
            case ONE:
                return TWO;
            case TWO:
                return B.THREE;

            default:
                throw new IllegalArgumentException(a.toString());
            }
        }

