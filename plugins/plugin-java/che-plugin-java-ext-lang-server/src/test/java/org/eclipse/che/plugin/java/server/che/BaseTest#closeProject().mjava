  @After
  public void closeProject() throws Exception {
    if (project != null) {
      project.close();
    }
    File pref = new File(wsPath + "/test/.che/project.preferences");
    if (pref.exists()) {
      pref.delete();
    }
  }

