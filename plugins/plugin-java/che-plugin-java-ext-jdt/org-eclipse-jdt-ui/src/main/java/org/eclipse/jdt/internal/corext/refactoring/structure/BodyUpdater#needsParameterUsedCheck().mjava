  /**
   * Returns whether {@link ChangeSignatureProcessor} should check if deleted parameters are
   * currently used in the method body.
   *
   * @return <code>true</code> by default, subclasses can override
   */
  public boolean needsParameterUsedCheck() {
    return true;
  }

