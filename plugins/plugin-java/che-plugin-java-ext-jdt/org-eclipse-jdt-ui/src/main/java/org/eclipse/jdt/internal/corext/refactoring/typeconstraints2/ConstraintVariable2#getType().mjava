	/**
	 * @return the type binding, or <code>null</code> iff the type constraint variable has no type in the original source (e.g. {@link
	 * CollectionElementVariable2})
	 */
	public TType getType() {
		return fType;
	}

