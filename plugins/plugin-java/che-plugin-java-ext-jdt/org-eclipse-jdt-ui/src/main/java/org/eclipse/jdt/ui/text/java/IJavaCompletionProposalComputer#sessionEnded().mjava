	/**
	 * Informs the computer that a content assist session has ended. This call will always be after
	 * any calls to
	 * {@linkplain #computeCompletionProposals(ContentAssistInvocationContext, org.eclipse.core.runtime.IProgressMonitor) computeCompletionProposals}
	 * and
	 * {@linkplain #computeContextInformation(ContentAssistInvocationContext, org.eclipse.core.runtime.IProgressMonitor) computeContextInformation}.
	 */
	void sessionEnded();

