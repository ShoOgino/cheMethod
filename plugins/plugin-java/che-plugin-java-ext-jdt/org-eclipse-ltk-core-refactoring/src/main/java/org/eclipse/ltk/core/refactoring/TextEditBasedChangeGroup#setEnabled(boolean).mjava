  /**
   * Marks the group as enabled or disabled. If a group is marked as disabled the text edits managed
   * by the underlying text edit group aren't executed when performing the text change that owns
   * this group.
   *
   * @param enabled <code>true</code> to mark this group as enabled, <code>false</code> to mark it
   *     as disabled
   */
  public void setEnabled(boolean enabled) {
    fIsEnabled = enabled;
  }

