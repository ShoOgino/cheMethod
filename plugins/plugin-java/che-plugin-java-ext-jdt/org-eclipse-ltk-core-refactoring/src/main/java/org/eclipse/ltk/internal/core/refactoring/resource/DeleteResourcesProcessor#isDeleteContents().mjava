  /**
   * Delete projects contents.
   *
   * @return <code>true</code> if this will delete the project contents. The content delete is not
   *     undoable.
   */
  public boolean isDeleteContents() {
    return fDeleteContents;
  }

