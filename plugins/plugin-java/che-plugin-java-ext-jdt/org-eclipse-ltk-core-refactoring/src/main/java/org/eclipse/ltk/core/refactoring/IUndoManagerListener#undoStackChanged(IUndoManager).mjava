  /**
   * This method is called by the undo manager if the undo stack has changed (for example a undo
   * object got added or the undo stack got flushed).
   *
   * @param manager the manager this listener is registered to
   */
  public void undoStackChanged(IUndoManager manager);

