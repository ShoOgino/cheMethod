  /**
   * Creates a new query.
   *
   * @param context the factory adapts the context to a <code>org.eclipse.swt.widgets.Shell</code>
   *     that is to be used to parent any dialogs with the user; use <code>null</code> if there is
   *     no UI context
   * @return the new query
   */
  public IValidationCheckResultQuery create(IAdaptable context);

