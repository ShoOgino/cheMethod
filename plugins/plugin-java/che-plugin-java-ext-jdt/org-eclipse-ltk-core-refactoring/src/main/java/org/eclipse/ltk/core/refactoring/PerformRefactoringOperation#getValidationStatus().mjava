  /**
   * Returns the refactoring status of the change's validation checking or <code>null</code> if a
   * change couldn't be created or the operation hasn't been performed yet.
   *
   * @return the refactoring status of the change's validation checking
   */
  public RefactoringStatus getValidationStatus() {
    return fValidationStatus;
  }

