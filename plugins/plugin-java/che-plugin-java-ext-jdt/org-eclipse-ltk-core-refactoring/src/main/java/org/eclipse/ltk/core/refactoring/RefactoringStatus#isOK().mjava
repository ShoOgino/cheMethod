  /**
   * Returns whether the status's severity is <code>OK</code> or not.
   *
   * @return <code>true</code> if the severity is <code>OK</code>; otherwise <code>false</code> is
   *     returned
   */
  public boolean isOK() {
    return fSeverity == OK;
  }

