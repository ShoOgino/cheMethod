  /**
   * Returns whether the undo proceeds or not. This method is called if the validation check
   * returned a status greater than <code>OK</code> and less than <code>FATAL</code>.
   *
   * @param status the refactoring status returned from {@link Change#isValid(IProgressMonitor)}
   * @return <code>true</code> if the undo should proceed; <code>false</code> otherwise
   */
  public boolean proceed(RefactoringStatus status);

