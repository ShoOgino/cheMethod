  /**
   * Return a boolean indicating whether this state represents an existent resource.
   *
   * @param checkMembers Use <code>true</code> if members should also exist in order for this state
   *     to be considered existent. A value of <code>false</code> indicates that the existence of
   *     members does not matter.
   * @return a boolean indicating whether this state represents an existent resource.
   */
  public abstract boolean verifyExistence(boolean checkMembers);

