  /**
   * This method is called by the undo manager if the redo stack has changed (for example a redo
   * object got added or the redo stack got flushed).
   *
   * @param manager the manager this listener is registered to
   */
  public void redoStackChanged(IUndoManager manager);

