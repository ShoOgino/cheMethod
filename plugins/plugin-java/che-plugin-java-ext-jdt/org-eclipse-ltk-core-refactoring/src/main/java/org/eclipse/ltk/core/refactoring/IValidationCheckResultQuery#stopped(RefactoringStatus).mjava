  /**
   * Called when the validation check returned a fatal error. In this case the undo can't proceed.
   * The hook can be used to present a corresponding dialog to the user.
   *
   * @param status the refactoring status returned from {@link Change#isValid(IProgressMonitor)}
   */
  public void stopped(RefactoringStatus status);

