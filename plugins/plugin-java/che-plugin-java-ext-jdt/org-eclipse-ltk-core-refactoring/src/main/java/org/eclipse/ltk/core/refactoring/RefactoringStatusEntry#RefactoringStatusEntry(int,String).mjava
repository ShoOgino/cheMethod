  /**
   * Creates a new refactoring status entry. The context is set to <code>
   * null</code> the problem code is set to <code>NO_CODE</code>, the plug-in identifier is set to
   * <code>null</code> and the data pointer is set to <code>null</code> as well.
   *
   * @param severity the severity
   * @param msg the message
   */
  public RefactoringStatusEntry(int severity, String msg) {
    this(severity, msg, null);
  }

