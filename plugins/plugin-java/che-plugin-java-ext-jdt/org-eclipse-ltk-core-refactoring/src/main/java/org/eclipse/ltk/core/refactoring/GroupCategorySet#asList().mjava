  /**
   * Converts the group categories into a a unmodifiable list.
   *
   * @return an unmodifiable list containing all group categories
   */
  public List /*<GroupCategory>*/ asList() {
    return Collections.unmodifiableList(fContent);
  }

