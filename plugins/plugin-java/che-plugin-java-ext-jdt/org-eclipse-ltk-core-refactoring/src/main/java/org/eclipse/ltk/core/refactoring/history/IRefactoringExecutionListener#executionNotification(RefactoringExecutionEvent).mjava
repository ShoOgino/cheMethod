  /**
   * Gets called if a refactoring execution event happened.
   *
   * <p>Implementors of this method should not rely on a fixed set of event types.
   *
   * <p>The event object is valid only for the duration of this method.
   *
   * @param event the refactoring execution event
   */
  public void executionNotification(RefactoringExecutionEvent event);

