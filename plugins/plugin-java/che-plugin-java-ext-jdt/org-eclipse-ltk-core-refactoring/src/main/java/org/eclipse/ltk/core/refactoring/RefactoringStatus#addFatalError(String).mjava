  /**
   * Adds a <code>FATAL</code> entry filled with the given message to this status. The severity of
   * this status will changed to <code>FATAL</code>.
   *
   * @param msg the message of the fatal entry
   * @see RefactoringStatusEntry
   */
  public void addFatalError(String msg) {
    addFatalError(msg, null);
  }

