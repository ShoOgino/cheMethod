  /**
   * The undo object or <code>null</code> if no undo exists. The undo object is initialize via the
   * call {@link Change#initializeValidationData(IProgressMonitor)}
   *
   * @return the undo object or <code>null</code>
   */
  public Change getUndoChange() {
    return fUndo;
  }

