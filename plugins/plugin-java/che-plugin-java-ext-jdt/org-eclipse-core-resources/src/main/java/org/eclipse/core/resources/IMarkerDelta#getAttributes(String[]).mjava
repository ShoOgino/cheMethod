  /**
   * Returns the attributes with the given names. The result is an array whose elements correspond
   * to the elements of the given attribute name array. Each element is <code>null</code> or an
   * instance of one of the following classes: <code>String</code>, <code>Integer</code>, or <code>
   * Boolean</code>.
   *
   * <p>If kind is <code>IResourceDelta.ADDED</code>, then the information is from the new marker,
   * otherwise it is from the old marker.
   *
   * @param attributeNames the names of the attributes
   * @return the values of the given attributes.
   */
  public Object[] getAttributes(String[] attributeNames);

