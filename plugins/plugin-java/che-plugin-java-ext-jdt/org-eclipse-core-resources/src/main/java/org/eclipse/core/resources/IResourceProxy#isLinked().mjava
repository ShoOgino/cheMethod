  /**
   * Returns whether the resource being visited is a linked resource.
   *
   * @return <code>true</code> if the resource is linked, and <code>false</code> otherwise
   * @see IResource#isLinked()
   */
  public boolean isLinked();

