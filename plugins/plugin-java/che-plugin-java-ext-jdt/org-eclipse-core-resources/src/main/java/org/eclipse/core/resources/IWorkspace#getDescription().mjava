  /**
   * Returns the workspace description. This object is responsible for defining workspace
   * preferences. The returned value is a modifiable copy but changes are not automatically applied
   * to the workspace. In order to changes take effect, <code>IWorkspace.setDescription</code> needs
   * to be called. The workspace description values are store in the preference store.
   *
   * @return the workspace description
   * @see #setDescription(IWorkspaceDescription)
   */
  public IWorkspaceDescription getDescription();

