  /**
   * Returns the full path of this file state. This refinement of the corresponding <code>IStorage
   * </code> method specifies that <code>IFileState</code>s always have a path and that path is the
   * full workspace path of the file represented by this state.
   *
   * @see IResource#getFullPath()
   * @see IStorage#getFullPath()
   */
  public IPath getFullPath();

