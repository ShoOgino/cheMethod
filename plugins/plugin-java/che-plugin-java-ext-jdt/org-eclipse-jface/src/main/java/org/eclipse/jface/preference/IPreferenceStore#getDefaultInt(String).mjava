  /**
   * Returns the default value for the integer-valued preference with the given name. Returns the
   * default-default value (<code>0</code>) if there is no default preference with the given name,
   * or if the default value cannot be treated as an integer.
   *
   * @param name the name of the preference
   * @return the default value of the named preference
   */
  public int getDefaultInt(String name);

