  /**
   * Create an empty dialog settings which loads and saves its content to a file. Use the methods
   * <code>load(String)</code> and <code>store(String)</code> to load and store this dialog
   * settings.
   *
   * @param sectionName the name of the section in the settings.
   */
  public DialogSettings(String sectionName) {
    name = sectionName;
    items = new HashMap<String, String>();
    arrayItems = new HashMap<String, String[]>();
    sections = new HashMap<String, IDialogSettings>();
  }

