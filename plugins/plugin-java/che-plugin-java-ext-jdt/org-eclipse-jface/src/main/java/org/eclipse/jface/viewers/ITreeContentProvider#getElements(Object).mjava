  /**
   * {@inheritDoc}
   *
   * <p><b>NOTE:</b> The returned array must not contain the given <code>inputElement</code>, since
   * this leads to recursion issues in {@link AbstractTreeViewer} (see <a
   * href="https://bugs.eclipse.org/9262">bug 9262</a>).
   */
  @Override
  public Object[] getElements(Object inputElement);

