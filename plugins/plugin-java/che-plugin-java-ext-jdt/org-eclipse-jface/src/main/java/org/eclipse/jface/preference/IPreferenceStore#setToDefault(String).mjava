  /**
   * Sets the current value of the preference with the given name back to its default value.
   *
   * <p>Note that the preferred way of re-initializing a preference to the appropriate default value
   * is to call <code>setToDefault</code>. This is implemented by removing the named value from the
   * store, thereby exposing the default value.
   *
   * @param name the name of the preference
   */
  public void setToDefault(String name);

