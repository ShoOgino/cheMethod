  /**
   * Returns the default value for the boolean-valued preference with the given name. Returns the
   * default-default value (<code>false</code>) if there is no default preference with the given
   * name, or if the default value cannot be treated as a boolean.
   *
   * @param name the name of the preference
   * @return the default value of the named preference
   */
  public boolean getDefaultBoolean(String name);

