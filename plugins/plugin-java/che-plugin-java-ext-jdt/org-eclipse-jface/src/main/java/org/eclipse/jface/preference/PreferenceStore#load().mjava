  /**
   * Loads this preference store from the file established in the constructor <code>
   * PreferenceStore(java.lang.String)</code> (or by <code>setFileName</code>). Default preference
   * values are not affected.
   *
   * @exception IOException if there is a problem loading this store
   */
  public void load() throws IOException {
    if (filename == null) {
      throw new IOException("File name not specified"); // $NON-NLS-1$
    }
    FileInputStream in = new FileInputStream(filename);
    load(in);
    in.close();
  }

