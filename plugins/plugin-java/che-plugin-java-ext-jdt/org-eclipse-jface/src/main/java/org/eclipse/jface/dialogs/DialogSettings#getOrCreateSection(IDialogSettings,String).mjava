  /**
   * Returns a section with the given name in the given dialog settings. If the section doesn't
   * exist yet, then it is first created.
   *
   * @param settings the parent settings
   * @param sectionName the name of the section
   * @return the section
   * @since 3.7
   */
  public static IDialogSettings getOrCreateSection(IDialogSettings settings, String sectionName) {
    IDialogSettings section = settings.getSection(sectionName);
    if (section == null) {
      section = settings.addNewSection(sectionName);
    }
    return section;
  }

