  @Before
  public void setUp() throws Exception {
    presenter =
        new OpenImplementationPresenter(
            javaNavigationService,
            appContext,
            dtoFactory,
            javaResources,
            popupResources,
            locale,
            editorAgent);
  }

