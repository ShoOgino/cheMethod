  /**
   * Returns the text range spawned by the given array of text edits. The method requires that the
   * given array contains at least one edit. If all edits passed are deleted the method returns
   * <code>
   * null</code>.
   *
   * @param edits an array of edits
   * @return the text range spawned by the given array of edits or <code>null</code> if all edits
   *     are marked as deleted
   */
  public static Region getCoverage(TextEdit[] edits) {
    // Assert.isTrue(edits != null && edits.length > 0);

    int offset = Integer.MAX_VALUE;
    int end = Integer.MIN_VALUE;
    int deleted = 0;
    for (int i = 0; i < edits.length; i++) {
      TextEdit edit = edits[i];
      if (edit.isDeleted()) {
        deleted++;
      } else {
        offset = Math.min(offset, edit.getOffset());
        end = Math.max(end, edit.getExclusiveEnd());
      }
    }
    if (edits.length == deleted) return null;

    return new RegionImpl(offset, end - offset);
  }

