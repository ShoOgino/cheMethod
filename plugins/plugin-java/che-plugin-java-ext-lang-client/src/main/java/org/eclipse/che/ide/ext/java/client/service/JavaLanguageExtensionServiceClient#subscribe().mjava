  private void subscribe() {
    transmitter
        .newRequest()
        .endpointId(WS_AGENT_JSON_RPC_ENDPOINT_ID)
        .methodName(EVENT_JDT_SUBSCRIBE)
        .noParams()
        .sendAndSkipResult();
  }

