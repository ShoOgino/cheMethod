    /**
     * Creates a new Line.
     *
     * @param offset
     *         the offset of the line
     * @param length
     *         the length of the line
     */
    public Line(int offset, int length) {
        this.offset = offset;
        this.length = length;
        this.delimiter = null;
    }

