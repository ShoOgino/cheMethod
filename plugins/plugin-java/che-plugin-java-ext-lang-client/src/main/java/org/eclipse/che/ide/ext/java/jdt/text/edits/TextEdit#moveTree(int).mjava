  /**
   * Move all offsets in the tree by the given delta. This node must be a root node. The resulting
   * offsets must be greater or equal to zero.
   *
   * @param delta the delta
   * @since 3.1
   */
  public final void moveTree(int delta) {
    // Assert.isTrue(fParent == null);
    // Assert.isTrue(getOffset() + delta >= 0);
    internalMoveTree(delta);
  }

