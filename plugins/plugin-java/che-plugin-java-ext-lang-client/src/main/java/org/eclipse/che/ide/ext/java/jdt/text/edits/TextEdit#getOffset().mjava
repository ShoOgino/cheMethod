  /**
   * Returns the offset of the edit. An offset is a 0-based character index. Returns <code>-1</code>
   * if the edit is marked as deleted.
   *
   * @return the offset of the edit
   */
  public int getOffset() {
    return fOffset;
  }

