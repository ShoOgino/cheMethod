    /**
     * Constructs a new move source edit.
     *
     * @param offset
     *         the edit's offset
     * @param length
     *         the edit's length
     */
    public MoveSourceEdit(int offset, int length) {
        super(offset, length);
    }

