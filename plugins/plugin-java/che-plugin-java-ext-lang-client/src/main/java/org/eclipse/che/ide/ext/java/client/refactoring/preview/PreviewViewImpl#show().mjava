    /** {@inheritDoc} */
    @Override
    public void show() {
        errorLabel.setText("");
        diff.clear();
        compare = null;
        treePanel.clear();
        containerChanges.clear();

        super.show();
    }

