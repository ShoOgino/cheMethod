  /**
   * Returns the document's line tracker. Assumes that the document has been initialized with a line
   * tracker.
   *
   * @return the document's line tracker
   */
  protected LineTracker getTracker() {
    Assert.isNotNull(fTracker);
    return fTracker;
  }

