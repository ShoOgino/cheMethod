  /**
   * Accepts the given visitor on the edits children.
   *
   * <p>This method must be used by the concrete implementations of <code>accept</code> to traverse
   * list-values properties; it encapsulates the proper handling of on-the-fly changes to the list.
   *
   * @param visitor the visitor object
   */
  protected final void acceptChildren(TextEditVisitor visitor) {
    if (fChildren == null) return;
    Iterator<TextEdit> iterator = fChildren.iterator();
    while (iterator.hasNext()) {
      TextEdit curr = iterator.next();
      curr.accept(visitor);
    }
  }

