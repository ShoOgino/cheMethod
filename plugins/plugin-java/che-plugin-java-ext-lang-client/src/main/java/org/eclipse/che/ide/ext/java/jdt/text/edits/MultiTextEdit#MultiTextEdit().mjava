  /**
   * Creates a new <code>MultiTextEdit</code>. The range of the edit is determined by the range of
   * its children.
   *
   * <p>Adding this edit to a parent edit sets its range to the range covered by its children. If
   * the edit doesn't have any children its offset is set to the parent's offset and its length is
   * set to 0.
   */
  public MultiTextEdit() {
    super(0, Integer.MAX_VALUE);
    fDefined = false;
  }

