  @Inject
  public CommitAction(
      CommitPresenter presenter,
      AppContext appContext,
      GitResources resources,
      GitLocalizationConstant constant) {
    super(
        constant.commitControlTitle(),
        constant.commitControlPrompt(),
        resources.commit(),
        appContext);
    this.presenter = presenter;
  }

