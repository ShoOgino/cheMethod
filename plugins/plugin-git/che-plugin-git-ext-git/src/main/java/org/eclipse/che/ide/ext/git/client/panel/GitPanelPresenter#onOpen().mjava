  /** Invoked each time when panel is activated. */
  @Override
  public void onOpen() {
    if (!initialized) {
      exposeGitRepositories();
      registerEventHandlers();

      initialized = true;
    }
  }

