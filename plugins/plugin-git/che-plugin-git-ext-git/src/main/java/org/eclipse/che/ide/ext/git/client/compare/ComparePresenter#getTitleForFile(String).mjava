  private String getTitleForFile(String file) {
    return "Review diff in: "
        + file
        + " ("
        + (currentFileIndex + 1)
        + '/'
        + alteredFiles.getFilesQuantity()
        + ')';
  }

