  @Override
  protected void onClose() {
    compare.getContent(
        new ContentCallBack() {
          @Override
          public void onContentReceived(String content) {
            delegate.onClose(content);
          }
        });
  }

