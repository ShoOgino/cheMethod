    private void subscribe() {
        requestTransmitter.newRequest()
                          .endpointId("ws-agent")
                          .methodName("track:git-checkout")
                          .noParams()
                          .sendAndSkipResult();
    }

