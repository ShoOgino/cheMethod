    @Override
    public void disarm() {
        super.disarm();

        presenter = new AddToIndexPresenter(view,
                                            appContext,
                                            constant,
                                            gitOutputConsoleFactory,
                                            consolesPanelPresenter,
                                            service,
                                            notificationManager);
    }

