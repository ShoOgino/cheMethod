    @Override
    public void disarm() {
        super.disarm();

        presenter = new StatusCommandPresenter(service,
                                               appContext,
                                               gitOutputConsoleFactory,
                                               consolesPanelPresenter,
                                               constant,
                                               notificationManager);
    }

