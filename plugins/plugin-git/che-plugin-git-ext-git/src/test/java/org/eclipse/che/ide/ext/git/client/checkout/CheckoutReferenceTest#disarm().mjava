  @Override
  public void disarm() {
    super.disarm();

    presenter =
        new CheckoutReferencePresenter(
            view,
            service,
            constant,
            notificationManager,
            gitOutputConsoleFactory,
            processesPanelPresenter,
            dtoFactory);
  }

