    @Override
    public void disarm() {
        super.disarm();

        presenter = new CheckoutReferencePresenter(view,
                                                   service,
                                                   appContext,
                                                   constant,
                                                   notificationManager,
                                                   gitOutputConsoleFactory,
                                                   consolesPanelPresenter,
                                                   dtoFactory);
    }

