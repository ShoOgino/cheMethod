    @Override
    public void onSaveClicked() {
        if (selectedTarget.getRecipe() == null) {
            createTargetRecipe();
        } else {
            updateTargetRecipe();
        }
    }

