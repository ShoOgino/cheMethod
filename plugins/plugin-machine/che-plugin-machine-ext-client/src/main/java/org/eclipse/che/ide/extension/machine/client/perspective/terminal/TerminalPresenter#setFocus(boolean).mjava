    @Override
    public void setFocus(boolean focused) {
        if (connected) {
            if (focused) {
                terminal.focus();
            } else {
                terminal.blur();
            }
        }
    }

