    @Override
    public String getTerminalUrl() {
        for (Link link : machineLinks) {
            if (TERMINAL_REFERENCE.equals(link.getRel())) {
                return link.getHref();
            }
        }
        return "";
    }

