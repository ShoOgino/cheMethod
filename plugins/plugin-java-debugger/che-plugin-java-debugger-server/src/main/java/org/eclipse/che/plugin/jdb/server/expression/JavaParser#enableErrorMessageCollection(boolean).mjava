  /**
   * Switches error message collection on or of.
   *
   * <p>The standard destination for parser error messages is <code>System.err</code>. However, if
   * <code>true</code> gets passed to this method this default behaviour will be switched off and
   * all error messages will be collected instead of written to anywhere.
   *
   * <p>The default value is <code>false</code>.
   *
   * @param pNewState <code>true</code> if error messages should be collected.
   */
  public void enableErrorMessageCollection(boolean pNewState) {
    mMessageCollectionEnabled = pNewState;
    if (mMessages == null && mMessageCollectionEnabled) {
      mMessages = new ArrayList<String>();
    }
  }

