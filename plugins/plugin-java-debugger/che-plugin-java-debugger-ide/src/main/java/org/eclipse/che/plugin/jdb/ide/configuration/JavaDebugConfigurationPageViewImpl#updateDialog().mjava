    private void updateDialog() {
        boolean connectToDevMachine = devHost.getValue();
        if (connectToDevMachine) {
            updatePortsList();
            port.setFocus(true);
        } else {
            host.selectAll();
            host.setFocus(true);
            port.clear();
        }
    }

