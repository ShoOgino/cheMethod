    public AbstractPHPUnitElement(final Map<?, ?> properties, final AbstractPHPUnitElement parent) {
        if (properties != null)
            init(properties);
        if (parent != null)
            setParent(parent);
    }

