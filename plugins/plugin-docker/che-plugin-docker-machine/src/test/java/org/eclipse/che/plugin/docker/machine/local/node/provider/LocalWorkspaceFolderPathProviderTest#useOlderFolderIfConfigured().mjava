  @Test
  public void useOlderFolderIfConfigured() throws Exception {
    LocalWorkspaceFolderPathProvider provider =
        new LocalWorkspaceFolderPathProvider(
            workspacesRoot, oldWorkspacesRoot, null, workspaceManagerProvider, false, false);

    provider.init();
    String providerPath = provider.getPath(WS_ID);

    assertEquals(providerPath, Paths.get(oldWorkspacesRoot, WS_NAME).toString());
  }

