  @Override
  protected void configure() {
    bind(new TypeLiteral<String[]>() {})
        .annotatedWith(Names.named("che.docker.dns_resolvers"))
        .toProvider(DnsResolversProvider.class);
  }

