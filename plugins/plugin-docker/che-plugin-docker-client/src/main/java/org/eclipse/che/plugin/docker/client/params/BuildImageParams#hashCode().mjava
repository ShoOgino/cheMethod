  @Override
  public int hashCode() {
    return Objects.hash(
        repository,
        tag,
        authConfigs,
        doForcePull,
        memoryLimit,
        memorySwapLimit,
        files,
        dockerfile,
        remote,
        quiet,
        noCache,
        removeIntermediateContainer,
        forceRemoveIntermediateContainers,
        cpusetCpus,
        cpuPeriod,
        cpuQuota,
        buildArgs);
  }

