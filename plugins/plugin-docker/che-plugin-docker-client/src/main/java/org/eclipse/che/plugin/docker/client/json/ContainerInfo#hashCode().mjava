  @Override
  public int hashCode() {
    return Objects.hash(
        id,
        created,
        appArmorProfile,
        path,
        Arrays.hashCode(args),
        config,
        state,
        image,
        networkSettings,
        resolvConfPath,
        hostConfig,
        driver,
        execDriver,
        hostnamePath,
        hostsPath,
        mountLabel,
        name,
        processLabel,
        Arrays.hashCode(execIDs),
        restartCount,
        logPath,
        node,
        volumes,
        volumesRW);
  }

