  @Override
  public int hashCode() {
    return Objects.hash(
        isRunning(),
        getPid(),
        getExitCode(),
        getStartedAt(),
        getFinishedAt(),
        isPaused(),
        isRestarting(),
        isDead(),
        oOMKilled,
        getError(),
        getStatus());
  }

