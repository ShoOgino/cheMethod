    @Test
    public void shouldGetXRegistryAuthConfigValue2() {
        String base64HeaderValue = authResolver.getXRegistryAuthHeaderValue(INITIAL_REGISTRY2_URL, customAuthConfigs);

        assertEqualsXRegistryAuthHeader(base64ToAuthConfig(base64HeaderValue),
                                        jsonToAuthConfig(INITIAL_REGISTRY2_X_REGISTRY_AUTH_HEADER_VALUE));
    }

