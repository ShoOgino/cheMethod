    @Override
    public void show() {
        super.show();
        new Timer() {
            @Override
            public void run() {
                if (isVisibleServer()) {
                    serverAddress.setFocus(true);
                } else {
                    username.setFocus(true);
                }
            }
        }.schedule(300);
    }

