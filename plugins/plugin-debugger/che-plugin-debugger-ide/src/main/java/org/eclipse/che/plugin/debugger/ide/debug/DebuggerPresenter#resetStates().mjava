  private void resetStates() {
    variables.clear();
    view.setVariables(variables);
    view.setVMName("");
    view.setExecutionPoint(null);
    selectedVariable = null;
    executionPoint = null;
  }

