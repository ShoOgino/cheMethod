    /** Calls special method on view which shows dialog window. */
    public void showDialog() {
        view.show();
        setPortsList();

        new Timer() {
            @Override
            public void run() {
                view.focus();
            }
        }.schedule(300);
    }

