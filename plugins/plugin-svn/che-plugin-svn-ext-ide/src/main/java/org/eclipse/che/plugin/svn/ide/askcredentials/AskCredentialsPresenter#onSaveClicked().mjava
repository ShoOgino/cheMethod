    @Override
    public void onSaveClicked() {
        saveCredentials(view.getUsername(), view.getPassword());
        view.clearUsername();
        view.clearPassword();
        view.close();
    }

