        @Override
        public boolean isValid() {
            if (target == null) {
                view.showErrorMarker(constants.copyEmptyTarget());
                return false;
            }

            return true;
        }

