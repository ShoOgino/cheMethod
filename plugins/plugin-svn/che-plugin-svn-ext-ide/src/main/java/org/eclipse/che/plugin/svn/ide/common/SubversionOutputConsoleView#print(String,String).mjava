    /**
     * Print text in a given color in view.
     *
     * @param text
     *         The text to display
     * @param color
     *         The color to use
     */
    void print(final String text, final String color);

