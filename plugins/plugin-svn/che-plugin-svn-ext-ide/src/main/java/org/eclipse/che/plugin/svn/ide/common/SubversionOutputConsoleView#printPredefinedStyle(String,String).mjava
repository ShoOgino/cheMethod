    /**
     * Print text with a given style in view.
     *
     * @param text
     *         The text to display
     * @param style
     *         The style to use
     */
    void printPredefinedStyle(final String text, final String style);

