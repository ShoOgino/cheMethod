        @Override
        public boolean isLeaf() {
            if ("file".equals(data.getNodeKind())) {
                return true;
            }

            if (children != null && children.isEmpty()) {
                return true;
            }

            return false;
        }

