  @Override
  public Promise<InfoResponse> info(
      Path project, String target, String revision, boolean children) {
    return info(project, target, revision, children, dtoFactory.createDto(Credentials.class));
  }

