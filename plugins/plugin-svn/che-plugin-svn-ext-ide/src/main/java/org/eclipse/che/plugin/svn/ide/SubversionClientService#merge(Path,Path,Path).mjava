  /**
   * Merge specified URL with target.
   *
   * @param project project path
   * @param target target directory
   * @param sourceUrl source URL to merge
   */
  Promise<CLIOutputResponse> merge(Path project, Path target, Path sourceUrl);

