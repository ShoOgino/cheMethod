  /**
   * Retrieves the information about repository item.
   *
   * @param project relative path to the project in local workspace
   * @param target target to operate
   * @param revision revision, use HEAD to specify latest revision
   * @param credentials {@link Credentials} object that contains user name and password for
   *     authentication
   */
  Promise<InfoResponse> info(
      Path project,
      String target,
      String revision,
      boolean children,
      @Nullable Credentials credentials);

