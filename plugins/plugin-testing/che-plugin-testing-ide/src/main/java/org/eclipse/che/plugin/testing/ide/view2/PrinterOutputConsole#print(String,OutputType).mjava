    @Override
    public void print(String text, OutputType type) {
        if (type == OutputType.STDERR) {
            print(text, false, "#FF4343");
        } else {
            print(text, false);
        }
    }

