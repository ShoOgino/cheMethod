    /**
     * Pushes an element onto the stack at the head of this deque.
     *
     * @param state
     *         new test state
     */
    public void push(TestState state) {
        stack.push(state);
    }

