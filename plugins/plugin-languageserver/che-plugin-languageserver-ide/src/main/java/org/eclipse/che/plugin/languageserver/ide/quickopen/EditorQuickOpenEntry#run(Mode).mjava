  @Override
  public boolean run(Mode mode) {
    if (mode == Mode.OPEN) {
      String filePath = getFilePath();
      editorHelper.openPath(filePath, getTextRange());
      return true;
    }
    return false;
  }

