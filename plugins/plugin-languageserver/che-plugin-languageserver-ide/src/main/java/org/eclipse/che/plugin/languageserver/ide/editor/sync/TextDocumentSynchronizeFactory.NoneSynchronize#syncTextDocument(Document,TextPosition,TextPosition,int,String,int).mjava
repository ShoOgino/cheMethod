    @Override
    public void syncTextDocument(
        Document document,
        TextPosition start,
        TextPosition end,
        int removedChars,
        String insertedText,
        int version) {
      // no-op implementation
    }

