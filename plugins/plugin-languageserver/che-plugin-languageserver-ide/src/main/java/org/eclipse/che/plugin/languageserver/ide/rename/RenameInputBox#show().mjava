  @Override
  public void show() {
    super.show();
    Scheduler.get()
        .scheduleDeferred(
            () -> {
              valueTextBox.selectAll();
              valueTextBox.setFocus(true);
            });
  }

