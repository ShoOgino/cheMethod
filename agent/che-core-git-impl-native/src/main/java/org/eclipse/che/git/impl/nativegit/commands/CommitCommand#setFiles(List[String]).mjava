    /**
     * Sets the list of iles to commit (ignores index).
     *
     * @param files
     *         the files to commit
     * @return this object
     */
    public CommitCommand setFiles(List<String> files) {
        this.files = files;
        return this;
    }

