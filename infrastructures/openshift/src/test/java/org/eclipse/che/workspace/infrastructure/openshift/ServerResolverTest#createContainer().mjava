    private Container createContainer() {
        return new ContainerBuilder().withPorts(new ContainerPortBuilder()
                                                        .withContainerPort(CONTAINER_PORT)
                                                        .build())
                                     .build();
    }

