  @BeforeMethod
  public void setUp() {
    osInfraProvisioner =
        new OpenShiftInfrastructureProvisioner(
            pvcProvisioner,
            uniqueNamesProvisioner,
            tlsRouteProvisioner,
            serversProvisioner,
            envVarsProvisioner,
            restartPolicyRewriter,
            labelsProvisioner);
    provisionOrder =
        inOrder(
            pvcProvisioner,
            uniqueNamesProvisioner,
            tlsRouteProvisioner,
            serversProvisioner,
            envVarsProvisioner,
            restartPolicyRewriter,
            labelsProvisioner);
  }

