    public DockerContainerConfig setBuild(DockerBuildContext build) {
        if (build != null) {
            build = new DockerBuildContext(build);
        }
        this.build = build;
        return this;
    }

