  private InstallerImpl newInstaller(int i) {
    return new InstallerImpl(
        "installer_" + i,
        "installer_name" + i,
        String.valueOf(i),
        "test installer",
        Collections.emptyList(),
        emptyMap(),
        "echo hello",
        emptyMap());
  }

