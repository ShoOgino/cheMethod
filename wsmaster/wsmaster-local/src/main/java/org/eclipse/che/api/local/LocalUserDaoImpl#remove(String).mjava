    @Override
    public void remove(String id) {
        requireNonNull(id);
        rwLock.writeLock().lock();
        try {
            users.remove(id);
        } finally {
            rwLock.writeLock().unlock();
        }
    }

