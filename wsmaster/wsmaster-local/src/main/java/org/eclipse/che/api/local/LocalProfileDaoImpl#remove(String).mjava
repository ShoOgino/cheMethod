    @Override
    public void remove(String id) {
        requireNonNull(id, "Required non-null id");
        lock.writeLock().lock();
        try {
           profiles.remove(id);
        } finally {
            lock.writeLock().unlock();
        }
    }

