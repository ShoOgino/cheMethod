    private void withMutex(Runnable action) {
        if (mutex == null) {
            action.run();
        } else {
            synchronized (mutex) {
                action.run();
            }
        }
    }

