  @BeforeMethod
  public void setUp() {
    devfileConverter =
        new DevfileConverter(
            projectConverter,
            commandConverter,
            ImmutableSet.of(toolProvisioner),
            ImmutableMap.of(TOOL_TYPE, toolToWorkspaceApplier));
  }

