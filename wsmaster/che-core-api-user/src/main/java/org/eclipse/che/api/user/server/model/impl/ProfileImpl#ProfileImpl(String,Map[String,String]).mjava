    public ProfileImpl(String userId, Map<String, String> attributes) {
        this.userId = userId;
        if (attributes != null) {
            this.attributes = new HashMap<>(attributes);
        }
    }

