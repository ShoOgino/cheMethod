  private static Long minOf(Long... values) {
    Long min = null;
    for (Long v : values) {
      if (v == null) {
        continue;
      }

      if (min == null || v < min) {
        min = v;
      }
    }

    return min;
  }

