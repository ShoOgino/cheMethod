  private static Long maxOf(Long... values) {
    Long max = null;
    for (Long v : values) {
      if (v == null) {
        continue;
      }

      if (max == null || v > max) {
        max = v;
      }
    }

    return max;
  }

