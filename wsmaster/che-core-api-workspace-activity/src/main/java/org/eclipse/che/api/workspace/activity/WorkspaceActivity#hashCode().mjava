  @Override
  public int hashCode() {
    return Objects.hash(
        workspaceId,
        created,
        lastStarting,
        lastRunning,
        lastStopping,
        lastStopped,
        expiration,
        status);
  }

