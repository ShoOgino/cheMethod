    @PostConstruct
    private void postConstruct() {
        subscriptionManager.register(MACHINE_LOG_METHOD,
                                     MachineLogEvent.class,
                                     this::predicate);
    }

