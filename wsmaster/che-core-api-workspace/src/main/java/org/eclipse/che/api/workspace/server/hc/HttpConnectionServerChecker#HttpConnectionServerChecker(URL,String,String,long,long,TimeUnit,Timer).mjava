    public HttpConnectionServerChecker(URL url,
                                       String machineName,
                                       String serverRef,
                                       long period,
                                       long timeout,
                                       TimeUnit timeUnit,
                                       Timer timer) {
        super(machineName, serverRef, period, timeout, timeUnit, timer);
        this.url = url;
    }

