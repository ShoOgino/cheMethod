  @Override
  public int hashCode() {
    return Objects.hash(
        getName(),
        getPublisher(),
        getId(),
        getVersion(),
        getContainers(),
        getInitContainers(),
        getEndpoints(),
        getWorkspaceEnv());
  }

