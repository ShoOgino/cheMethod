    public UsersWorkspaceImpl(WorkspaceConfig config,
                              String id,
                              String owner) {
        this.id = id;
        this.owner = owner;
        this.workspaceConfig = new WorkspaceConfigImpl(config);
    }

