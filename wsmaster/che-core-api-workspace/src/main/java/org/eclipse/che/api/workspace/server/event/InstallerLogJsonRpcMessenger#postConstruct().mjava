    @PostConstruct
    private void postConstruct() {
        subscriptionManager.register(INSTALLER_LOG_METHOD,
                                     InstallerLogEvent.class,
                                     this::predicate);
    }

