  public DevfileImpl(Devfile devfile) {
    this(
        devfile.getSpecVersion(),
        devfile.getName(),
        devfile.getProjects(),
        devfile.getComponents(),
        devfile.getCommands(),
        devfile.getAttributes());
  }

