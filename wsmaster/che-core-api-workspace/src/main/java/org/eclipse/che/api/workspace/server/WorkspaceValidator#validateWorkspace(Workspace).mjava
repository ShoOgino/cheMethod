  /**
   * Checks that workspace is valid.
   *
   * @param workspace workspace configuration for validation
   * @throws BadRequestException in the case of constrain violation
   * @throws ServerException when constraint can not be validated because of network or other errors
   */
  void validateWorkspace(Workspace workspace) throws BadRequestException, ServerException;

