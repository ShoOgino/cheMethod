  /** Syncs {@link #name} with config name. */
  private void syncName() {
    if (devfile != null) {
      name = devfile.getName();
    } else if (config != null) {
      name = config.getName();
    } else {
      name = null;
    }
  }

