    protected long getIdleTimeout(String workspaceId) throws NotFoundException, ServerException {
        if (timeout > 0) {
            return timeout;
        } else {
            return -1;
        }
    }

