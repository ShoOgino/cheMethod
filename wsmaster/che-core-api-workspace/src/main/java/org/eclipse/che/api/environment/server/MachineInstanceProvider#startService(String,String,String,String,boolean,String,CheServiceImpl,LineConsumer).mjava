  /**
   * Create docker container from compose service definition.
   *
   * @param ownerName name of user who starts provided service
   * @param workspaceId ID of workspace that owns provided service
   * @param envName name of environment that owns provided service
   * @param machineName name of machine which represents provided service
   * @param isDev whether service will contain ws-agent or not
   * @param networkName name of docker network which service should join
   * @param service description of docker compose service
   * @param machineLogger consumer of logs of service
   * @return machine instance
   * @throws ServerException if any error occurs
   */
  Instance startService(
      String ownerName,
      String workspaceId,
      String envName,
      String machineName,
      boolean isDev,
      String networkName,
      CheServiceImpl service,
      LineConsumer machineLogger)
      throws ServerException;

