  @Override
  public int hashCode() {

    return Objects.hash(
        generatedId,
        alias,
        type,
        componentId,
        reference,
        referenceContent,
        image,
        memoryLimit,
        getSelector(),
        getEntrypoints(),
        getMountSources(),
        getCommand(),
        getArgs(),
        getVolumes(),
        getEnv(),
        getEndpoints());
  }

