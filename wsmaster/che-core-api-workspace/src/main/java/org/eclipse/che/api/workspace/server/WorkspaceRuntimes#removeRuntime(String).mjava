    @VisibleForTesting
    void removeRuntime(String wsId) {
        rwLock.writeLock().lock();
        try {
            descriptors.remove(wsId);
        } finally {
            rwLock.writeLock().unlock();
        }
    }

