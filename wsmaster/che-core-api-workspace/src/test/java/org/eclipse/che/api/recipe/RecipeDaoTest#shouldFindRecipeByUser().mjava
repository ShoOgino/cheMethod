  @Test
  public void shouldFindRecipeByUser() throws Exception {
    final List<OldRecipeImpl> result = recipeDao.search(null, null, null, 0, recipes.size());

    assertTrue(result.contains(recipes.get(0)));
  }

