    private void checkRuntime() {
        if (internalRuntime != null) {
            throw new RuntimeException("internalRuntime field is not null. Looks like multiple runtimes were created.");
        }
    }

