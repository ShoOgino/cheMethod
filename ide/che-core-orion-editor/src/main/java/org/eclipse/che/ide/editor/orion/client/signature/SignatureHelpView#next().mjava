    private void next() {
        if (signatureViews.size() < 2) {
            return;
        }
        activeSignature = (activeSignature + 1) % signatureViews.size();
        select(activeSignature);
    }

