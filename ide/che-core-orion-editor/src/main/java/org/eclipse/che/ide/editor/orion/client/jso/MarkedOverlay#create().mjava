  private static Promise<MarkedOverlay> create() {
    return Promises.create(
        (resolve, reject) -> {
          create(
              (marked) -> {
                resolve.apply(marked);
              });
        });
  }

