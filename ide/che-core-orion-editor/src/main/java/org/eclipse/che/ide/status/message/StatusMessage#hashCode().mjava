  @Override
  public int hashCode() {
    int hash = 7;
    hash = 31 * hash + message.hashCode();
    hash = 31 * hash + type.hashCode();
    hash = 31 * hash + (isAccessible ? 1 : 0);
    return hash;
  }

