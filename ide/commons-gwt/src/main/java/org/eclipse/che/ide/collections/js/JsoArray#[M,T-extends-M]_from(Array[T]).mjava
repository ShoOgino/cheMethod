  /** Casts a Array to a JsoArray. Unsafe, but on client we know we always have a JsoArray. */
  @SuppressWarnings("unchecked")
  public static <M, T extends M> JsoArray<M> from(Array<T> array) {
    return (JsoArray<M>) array;
  }

