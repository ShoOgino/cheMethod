  /**
   * @param start
   * @param end
   * @return A sub-array starting at start (inclusive) and endint at the end index (exclusive).
   */
  @Override
  public final native JsoArray<T> slice(int start, int end) /*-{
        return this.slice(start, end);
    }-*/;

