  @Override
  public void remove() {
    if (hasRemovedSinceNextCall || index == 0) {
      throw new IllegalStateException();
    }

    index--;
    items.remove(index);
    hasRemovedSinceNextCall = true;
  }

