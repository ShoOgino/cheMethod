  /**
   * @return True if the event is the key combo for "undo" NOTE(danilatos): This is the best we have
   *     at detecting undo events :( We need even more special handling for undo done via the menu
   *     :( :(
   */
  boolean isUndoCombo();

