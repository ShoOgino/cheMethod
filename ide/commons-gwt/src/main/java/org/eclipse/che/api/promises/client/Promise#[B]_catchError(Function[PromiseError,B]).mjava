  /**
   * Adds actions when the promise is rejected.
   *
   * <p>The action is added both to the original promise and the returned value, but the promises
   * are not necessarily the same object.
   *
   * <p>This is shorthand for {@code promise.then(null, onRejected)}.
   *
   * <p>Note: the method name does not match the ES6 specification, obviously because <code>catch
   * </code> can't be used in Java.
   *
   * @param onRejected the rejection action added
   * @return a promise equivalent to the original promise with the rejection action added
   */
  <B> Promise<B> catchError(Function<PromiseError, B> onRejected);

