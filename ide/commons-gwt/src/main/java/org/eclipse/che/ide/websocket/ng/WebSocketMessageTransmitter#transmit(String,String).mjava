    /**
     * Transmits WEB SOCKET messages
     *
     * @param protocol
     *         message protocol
     * @param message
     *         message body
     */
    void transmit(String protocol, String message);

