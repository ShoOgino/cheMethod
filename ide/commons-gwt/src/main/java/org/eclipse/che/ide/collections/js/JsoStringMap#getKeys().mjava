  @Override
  public final native JsoArray<String> getKeys() /*-{
        keys = [];
        for (key in this) {
            if (Object.prototype.hasOwnProperty.call(this, key)) {
                keys.push(key);
            }
        }
        return keys;
    }-*/;

