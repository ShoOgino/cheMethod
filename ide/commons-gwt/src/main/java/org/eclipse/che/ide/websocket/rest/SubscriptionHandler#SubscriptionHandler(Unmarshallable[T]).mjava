  /**
   * Constructor retrieves unmarshaller with initialized (this is important!) object. When response
   * comes callback calls <code>Unmarshallable.unmarshal()</code> which populates the object.
   *
   * @param unmarshaller {@link Unmarshallable}
   */
  public SubscriptionHandler(Unmarshallable<T> unmarshaller) {
    this.unmarshaller = unmarshaller;
  }

