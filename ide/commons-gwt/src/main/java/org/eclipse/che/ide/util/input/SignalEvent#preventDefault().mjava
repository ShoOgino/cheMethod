  /**
   * Prevents the browser from taking its default action for the given event. Under some
   * circumstances (see {@link SignalEventImpl#isPreventDefaultEffective} this may also stop
   * propagation.
   *
   * <p>See also {@link #stopPropagation()}.
   */
  public void preventDefault();

