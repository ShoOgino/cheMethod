  /**
   * TODO(user): Deprecate this, as it breaks abstraction. Also prevent people from casting back and
   * forth.
   *
   * @return The underlying event view of this event
   */
  Event asEvent();

