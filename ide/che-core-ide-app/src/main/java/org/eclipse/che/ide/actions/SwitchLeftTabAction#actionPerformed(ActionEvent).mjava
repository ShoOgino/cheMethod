    /** {@inheritDoc} */
    @Override
    public void actionPerformed(ActionEvent event) {
        EditorPartPresenter editor = editorAgent.getPreviousEditor();
        if (editor == null) {
            editor = editorAgent.getLastEditor();
        }
        editorPartStackPresenter.setActivePart(editor);
    }

