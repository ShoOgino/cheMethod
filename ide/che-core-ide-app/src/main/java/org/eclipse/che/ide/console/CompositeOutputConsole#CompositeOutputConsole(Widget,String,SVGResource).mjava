  @Inject
  public CompositeOutputConsole(
      @Assisted Widget widget, @Assisted String title, @Assisted SVGResource icon) {
    this.widget = widget;
    this.title = title;
    this.icon = icon;
  }

