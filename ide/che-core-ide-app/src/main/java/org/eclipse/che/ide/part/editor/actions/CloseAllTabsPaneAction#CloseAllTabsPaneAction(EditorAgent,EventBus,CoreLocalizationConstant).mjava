  @Inject
  public CloseAllTabsPaneAction(
      EditorAgent editorAgent, EventBus eventBus, CoreLocalizationConstant locale) {
    super(
        locale.editorCloseAllTabsInPane(),
        locale.editorCloseAllTabsInPaneDescription(),
        null,
        editorAgent,
        eventBus);
  }

