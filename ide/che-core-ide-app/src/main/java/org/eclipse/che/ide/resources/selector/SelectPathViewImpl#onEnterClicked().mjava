    @Override
    protected void onEnterClicked() {
        if (isWidgetFocused(submitBtn)) {
            delegate.onSubmit();
            hide();
            return;
        }

        if (isWidgetFocused(cancelButton)) {
            delegate.onCancel();
            hide();
        }
    }

