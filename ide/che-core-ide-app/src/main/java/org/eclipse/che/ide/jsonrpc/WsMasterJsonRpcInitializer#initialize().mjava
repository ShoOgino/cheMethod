    private void initialize() {
        String workspaceMasterUrl = getWsMasterURL();

        initializer.initialize(WS_MASTER_JSON_RPC_ENDPOINT_ID, singletonMap("url", workspaceMasterUrl), singleton(this::processWsId));
    }

