    private Node getParentNode(Path path) {
        Node node = null;

        while (node == null) {
            if (path.segmentCount() == 0) {
                return null;
            }

            path = path.parent();
            node = getNode(path);
        }

        return node;
    }

