    @Override
    public List<CommandImpl> getCommands() {
        return commands.values()
                       .stream()
                       .map(CommandImpl::new)
                       .collect(toList());
    }

