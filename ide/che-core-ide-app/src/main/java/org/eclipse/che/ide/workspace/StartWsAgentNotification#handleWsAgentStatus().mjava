  private void handleWsAgentStatus() {
    wsAgentServerUtil
        .getWsAgentHttpServer()
        .ifPresent(
            server -> {
              if (server.getStatus() == STOPPED) {
                show();
              }
            });
  }

