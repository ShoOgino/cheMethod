    private void register(ProjectTemplateDescriptor descriptor) {
        templateDescriptors.computeIfAbsent(descriptor.getProjectType(), key -> new ArrayList<>())
                           .add(descriptor);
    }

