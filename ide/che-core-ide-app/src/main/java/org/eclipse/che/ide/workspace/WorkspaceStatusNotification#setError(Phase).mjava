    /**
     * Sets phase filed.
     *
     * @param phase
     *         corresponding phase
     */
    void setError(Phase phase) {
        PopupLoader popup = popups.get(phase);
        if (popup != null) {
            // Don't hide the loader with status ERROR
            popups.remove(phase);
            popup.setError();
        }
    }

