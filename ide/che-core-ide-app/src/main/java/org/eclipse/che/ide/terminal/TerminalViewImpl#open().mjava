  private void open() {
    if (getElement().getOffsetWidth() > 0 && getElement().getOffsetHeight() > 0) {
      terminal.open(terminalPanel.getElement());
      if (focusOnOpen) {
        terminal.focus();
      }
      isOpen = true;
    }
  }

