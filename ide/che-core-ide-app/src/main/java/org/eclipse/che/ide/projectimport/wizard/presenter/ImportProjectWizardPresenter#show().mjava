  public void show() {
    resetState();

    wizard = createDefaultWizard();
    final WizardPage<MutableProjectConfig> firstPage = wizard.navigateToFirst();
    if (firstPage != null) {
      showPage(firstPage);
      view.showDialog();
    }
  }

