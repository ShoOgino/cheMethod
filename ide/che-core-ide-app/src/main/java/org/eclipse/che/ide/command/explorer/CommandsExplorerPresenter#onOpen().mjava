    @Override
    public void onOpen() {
        super.onOpen();

        if (isFirstActivation) {
            commandManager.fetchCommands();
            refreshView();
            isFirstActivation = false;
        }
    }

