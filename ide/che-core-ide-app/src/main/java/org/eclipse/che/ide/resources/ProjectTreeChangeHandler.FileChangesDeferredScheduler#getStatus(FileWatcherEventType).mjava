    private int getStatus(FileWatcherEventType eventType) {
      if (eventType == CREATED) {
        return ADDED;
      } else if (eventType == DELETED) {
        return REMOVED;
      } else {
        return UPDATED;
      }
    }

