  private void subscribe() {
    requestTransmitter
        .newRequest()
        .endpointId(WS_AGENT_JSON_RPC_ENDPOINT_ID)
        .methodName(EXCLUDES_SUBSCRIBE)
        .noParams()
        .sendAndSkipResult();
  }

