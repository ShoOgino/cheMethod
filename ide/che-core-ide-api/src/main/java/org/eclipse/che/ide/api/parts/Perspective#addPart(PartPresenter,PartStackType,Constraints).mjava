  /**
   * Opens part with special constraint.
   *
   * @param part part which will be opened
   * @param type part stack type to find it among active part stacks
   * @param constraint constraints with which need open part
   */
  void addPart(
      @NotNull PartPresenter part, @NotNull PartStackType type, @Nullable Constraints constraint);

