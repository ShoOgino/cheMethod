    public PoliciesImpl(String referrer,
                        String create,
                        Long until,
                        Long since) {
        this.referrer = referrer;
        this.create = create;
        this.until = until;
        this.since = since;
    }

