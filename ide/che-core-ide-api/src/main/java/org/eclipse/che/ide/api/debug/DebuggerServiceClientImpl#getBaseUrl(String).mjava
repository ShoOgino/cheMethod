    private String getBaseUrl(String id) {
        final String url = appContext.getDevAgentEndpoint() + "/debugger";
        if (id != null) {
            return url + "/" + id;
        }
        return url;
    }

