  /**
   * Opens new Part or shows already opened
   *
   * @param part part which need open
   * @param type type of part
   */
  void addPart(@NotNull PartPresenter part, @NotNull PartStackType type);

