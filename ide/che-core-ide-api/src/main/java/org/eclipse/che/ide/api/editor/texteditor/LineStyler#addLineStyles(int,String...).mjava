    /**
     * Adds the styles to the line.
     *
     * @param lineNumber
     *         the line number
     * @param styles
     *         the styles to add
     */
    void addLineStyles(int lineNumber, String... styles);

