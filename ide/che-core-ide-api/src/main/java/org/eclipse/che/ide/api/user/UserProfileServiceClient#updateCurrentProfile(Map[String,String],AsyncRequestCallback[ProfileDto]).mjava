  /**
   * Update current user's profile.
   *
   * @param updates attributes to update
   * @param callback
   */
  void updateCurrentProfile(
      @NotNull Map<String, String> updates, AsyncRequestCallback<ProfileDto> callback);

