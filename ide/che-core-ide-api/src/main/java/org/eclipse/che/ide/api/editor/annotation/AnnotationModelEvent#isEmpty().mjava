  /**
   * Returns whether this annotation model event is empty or not. If this event represents a world
   * change, this method returns <code>false</code> although the event does not carry any added,
   * removed, or changed annotations.
   *
   * @return <code>true</code> if this event is empty
   */
  public boolean isEmpty() {

    final boolean result =
        addedAnnotations.isEmpty() && removedAnnotations.isEmpty() && changedAnnotations.isEmpty();
    Log.debug(getClass(), "result", result);

    return result;
  }

