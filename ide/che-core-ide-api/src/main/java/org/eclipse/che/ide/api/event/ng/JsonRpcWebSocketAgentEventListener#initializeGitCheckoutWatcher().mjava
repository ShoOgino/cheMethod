  private void initializeGitCheckoutWatcher() {
    requestTransmitter
        .newRequest()
        .endpointId("ws-agent")
        .methodName("track/git-checkout")
        .noParams()
        .sendAndSkipResult();
  }

