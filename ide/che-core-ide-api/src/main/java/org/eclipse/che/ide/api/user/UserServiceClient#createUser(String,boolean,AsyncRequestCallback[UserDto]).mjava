  /**
   * Create new user.
   *
   * @param token user's token
   * @param isTemporary if <code>true</code> - is temporary user
   * @param callback
   */
  void createUser(
      @NotNull String token, boolean isTemporary, AsyncRequestCallback<UserDto> callback);

