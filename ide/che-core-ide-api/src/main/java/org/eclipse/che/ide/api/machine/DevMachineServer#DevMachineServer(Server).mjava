        public DevMachineServer(Server dto) {
            path = dto.getPath();
            address = dto.getAddress();
            protocol = dto.getProtocol();
            ref = dto.getRef();
            url = dto.getUrl();
        }

