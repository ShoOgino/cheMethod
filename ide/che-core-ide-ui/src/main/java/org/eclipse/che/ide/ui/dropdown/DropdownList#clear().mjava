    /** Clear the list. */
    public void clear() {
        itemListWidgets.clear();
        itemHeaderWidgets.clear();
        dropdownContentPanel.clear();

        adaptDropDownPanelHeight();
        checkListEmptiness();
    }

