    @Before
    public void setUp() throws Exception {
        when(resources.buttonCss()).thenReturn(runnerCss);

        widget = new ConsoleButtonImpl(resources, tooltip, SOME_MESSAGE, mock(SVGResource.class, RETURNS_MOCKS));
    }

