  public void waitSamplesForm() {
    waitButtons(
        asList(
            SAMPLES_BUTTON,
            BLANK_BUTTON,
            GIT_BUTTON,
            GITHUB_BUTTON,
            ZIP_BUTTON,
            CANCEL_BUTTON,
            ADD_BUTTON));
  }

