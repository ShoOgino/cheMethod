  /** open JavaDoc popup */
  public void openJavaDocPopUp() {
    loader.waitOnClosed();
    actionsFactory
        .createAction(seleniumWebDriver)
        .keyDown(CONTROL)
        .sendKeys(Keys.chord("q"))
        .keyUp(CONTROL)
        .perform();
  }

