  @Override
  public String toString() {
    return format(
        "%s{login=%s, email=%s, firstName=%s, lastName=%s}",
        this.getClass().getSimpleName(),
        this.getLogin(),
        this.getEmail(),
        getFirstName(),
        getLastName());
  }

