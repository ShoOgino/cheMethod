  private void deleteDependency() {
    editor.waitActive();
    loader.waitOnClosed();
    for (int i = 36; i <= 40; i++) {
      editor.setCursorToLine(i);
      editor.selectLineAndDelete();
    }
  }

