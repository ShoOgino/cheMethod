    /** {@inheritDoc} */
    @Override
    public void onClick(Notification notification) {
        NotificationListener listener = notification.getListener();
        if (listener != null) {
            listener.onClick();
        }
    }

