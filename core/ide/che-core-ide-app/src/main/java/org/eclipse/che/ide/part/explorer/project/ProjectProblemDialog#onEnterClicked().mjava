    /** {@inheritDoc} */
    @Override
    protected void onEnterClicked() {
        if (isWidgetFocused(configureButton)) {
            handler.onConfigure();
            onClose();
            return;
        }

        if (isWidgetFocused(keepBlankButton)) {
            handler.onKeepBlank();
            onClose();
        }
    }

