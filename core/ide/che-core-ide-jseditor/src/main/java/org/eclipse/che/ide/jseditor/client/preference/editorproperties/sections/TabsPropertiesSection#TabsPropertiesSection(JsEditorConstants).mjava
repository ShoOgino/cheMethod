    @Inject
    public TabsPropertiesSection(JsEditorConstants locale) {
        this.locale = locale;
        properties = Arrays.asList(TAB_SIZE.toString(),
                                   EXPAND_TAB.toString());
    }

