    @Override
    public int read() {
        if (this.offset == this.content.length()) {
            return EOF;
        }
        final int result = this.content.charAt(this.offset);
        this.offset++;
        return result;
    }

