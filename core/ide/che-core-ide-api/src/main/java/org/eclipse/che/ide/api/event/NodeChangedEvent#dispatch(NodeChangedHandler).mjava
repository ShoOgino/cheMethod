    /** {@inheritDoc} */
    @Override
    protected void dispatch(NodeChangedHandler handler) {
        switch (nodeAction) {
            case RENAMED:
                handler.onNodeRenamed(this);
                break;
            default:
                break;
        }
    }

