    /** @see Status#getMissing() */
    @Override
    public List<String> getMissing() {
        if (missing == null) {
            missing = new ArrayList<>();
        }
        return missing;
    }

