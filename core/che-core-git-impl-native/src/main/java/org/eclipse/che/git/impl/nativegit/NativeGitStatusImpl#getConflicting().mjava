    /** @see Status#getConflicting() */
    @Override
    public List<String> getConflicting() {
        if (conflicting == null) {
            conflicting = new ArrayList<>();
        }
        return conflicting;
    }

