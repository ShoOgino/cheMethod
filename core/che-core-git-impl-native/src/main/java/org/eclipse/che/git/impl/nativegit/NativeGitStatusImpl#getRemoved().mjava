    /** @see Status#getRemoved() */
    @Override
    public List<String> getRemoved() {
        if (removed == null) {
            removed = new ArrayList<>();
        }
        return removed;
    }

