    void doClose() {
        super.close();
        final java.io.File dir = getIndexDir();
        if (!deleteRecursive(dir)) {
            LOG.warn("Unable delete index directory '{}'", dir);
        }
    }

