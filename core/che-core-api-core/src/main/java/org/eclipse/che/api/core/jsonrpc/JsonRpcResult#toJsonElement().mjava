    public JsonElement toJsonElement() {
        if (result != null) {
            return result;
        }

        JsonArray jsonArray = new JsonArray();
        resultList.forEach(jsonArray::add);

        return jsonArray;
    }

