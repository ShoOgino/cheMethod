    /**
     * Creates an array of stringified JSON RPC structures, which can further
     * be unmarshalled separately.
     *
     * @param message
     *         incoming message
     * @return array of serialized JSON RPC
     */
    List<String> unmarshalArray(String message);

