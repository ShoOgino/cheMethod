    /**
     * Composes an object out of JSON RPC result entity. The resulting object
     * can be of the following types: {@link String}, {@link Boolean},
     * {@link Double} or DTO.
     *
     * @param result
     *         JSON RPC result
     * @param type
     *         type of resulting object
     * @param <T>
     *         generic type of resulting object
     * @return object of class T
     */
    <T> T composeOne(JsonRpcResult result, Class<T> type);

