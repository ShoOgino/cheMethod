    /**
     * Broadcasts WEB SOCKET messages to all endpoints
     *
     * @param protocol
     *         message protocol
     * @param message
     *         message body
     */
    void transmit(String protocol, String message);

