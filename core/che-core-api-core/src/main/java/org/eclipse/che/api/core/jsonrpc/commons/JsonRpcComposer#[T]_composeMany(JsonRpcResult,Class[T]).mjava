    /**
     * Composes a list of objects out of JSON RPC result entity. The resulting
     * objects can be of the following types: {@link String}, {@link Boolean},
     * {@link Double} or DTO.
     *
     * @param result
     *         JSON RPC params
     * @param type
     *         type of resulting objects
     * @param <T>
     *         generic type of resulting objects
     * @return list of objects of class T
     */
    <T> List<T> composeMany(JsonRpcResult result, Class<T> type);

