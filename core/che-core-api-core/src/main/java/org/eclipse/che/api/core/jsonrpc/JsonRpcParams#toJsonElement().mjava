    public JsonElement toJsonElement() {
        if (params != null) {
            return params;
        }

        JsonArray array = new JsonArray();
        paramsList.forEach(array::add);
        return array;
    }

