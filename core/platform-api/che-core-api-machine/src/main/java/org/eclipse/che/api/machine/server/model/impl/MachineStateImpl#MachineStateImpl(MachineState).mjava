    public MachineStateImpl(MachineState machine) {
        this(machine.isDev(),
             machine.getName(),
             machine.getType(),
             machine.getSource(),
             machine.getLimits(),
             machine.getId(),
             machine.getChannels(),
             machine.getWorkspaceId(),
             machine.getOwner(),
             machine.getEnvName(),
             machine.getStatus());
    }

