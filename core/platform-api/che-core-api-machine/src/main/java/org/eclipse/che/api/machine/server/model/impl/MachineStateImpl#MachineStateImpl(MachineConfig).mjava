    public MachineStateImpl(MachineConfig machine) {
        this(machine.isDev(),
             machine.getName(),
             machine.getType(),
             machine.getSource(),
             machine.getLimits(),
             null,
             null,
             null,
             null,
             null,
             null);
    }

