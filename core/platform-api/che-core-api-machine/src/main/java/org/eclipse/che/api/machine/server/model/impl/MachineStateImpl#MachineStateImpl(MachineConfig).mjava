    public MachineStateImpl(MachineConfig machine) {
        this(machine.isDev(),
             machine.getType(),
             machine.getName(),
             machine.getSource(),
             machine.getLimits(),
             null,
             null,
             null,
             null,
             null,
             null);
    }

