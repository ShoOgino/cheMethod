    public MachineImpl(Machine machine) {
        this(machine.isDev(),
             machine.getName(),
             machine.getType(),
             machine.getSource(),
             machine.getLimits(),
             machine.getId(),
             machine.getMetadata(),
             machine.getChannels(),
             machine.getWorkspaceId(),
             machine.getOwner(),
             machine.getEnvName(),
             machine.getStatus());
    }

