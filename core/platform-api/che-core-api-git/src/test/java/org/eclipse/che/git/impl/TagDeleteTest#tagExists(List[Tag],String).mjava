    private boolean tagExists(List<Tag> list, String name) {
        for (Tag tag : list) {
            if (tag.getName().equals(name)) {
                return true;
            }
        }
        return false;
    }

