    public Account(@NotNull Account account) {
        this(requireNonNull(account, "Required non-null account").id,
             account.name,
             account.workspaces,
             account.attributes);
    }

