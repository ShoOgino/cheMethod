    @Override
    public String toString() {
        if (asString == null) {
            if (isRoot()) {
                asString = "/";
            } else {
                asString = join('/');
            }
        }
        return asString;
    }

