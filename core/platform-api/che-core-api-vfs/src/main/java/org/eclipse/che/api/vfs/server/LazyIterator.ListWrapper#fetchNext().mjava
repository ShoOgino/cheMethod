        @Override
        protected void fetchNext() {
            next = null;
            while (next == null && delegate.hasNext()) {
                next = delegate.next();
            }
        }

