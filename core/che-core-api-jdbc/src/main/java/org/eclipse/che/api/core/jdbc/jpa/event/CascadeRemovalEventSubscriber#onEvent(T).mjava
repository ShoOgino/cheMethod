    @Override
    public void onEvent(T event) {
        if (!event.getContext().isFailed()) {
            try {
                onRemovalEvent(event);
            } catch (Exception ex) {
                event.getContext().fail(ex);
            }
        }
    }

