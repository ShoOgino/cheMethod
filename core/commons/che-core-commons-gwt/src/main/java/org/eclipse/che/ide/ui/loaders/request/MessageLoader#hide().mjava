    /** {@inheritDoc} */
    @Override
    public void hide() {
        if (showing) {
            glass.removeFromParent();
            showing = false;
            return;
        }

        showLoader.cancel();
    }

