    @Override
    public T next() {
        if (index == items.size()) {
            throw new NoSuchElementException();
        }

        T result = items.get(index);
        index++;

        hasRemovedSinceNextCall = false;

        return result;
    }

