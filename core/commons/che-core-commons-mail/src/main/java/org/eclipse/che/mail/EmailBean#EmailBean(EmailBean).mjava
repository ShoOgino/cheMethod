  public EmailBean(EmailBean email) {
    this(
        email.getFrom(),
        email.getTo(),
        email.getReplyTo(),
        email.getMimeType(),
        email.getBody(),
        email.getSubject(),
        email.getAttachments());
  }

