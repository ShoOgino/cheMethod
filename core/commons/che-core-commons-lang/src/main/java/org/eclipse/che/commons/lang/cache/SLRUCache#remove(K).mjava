    @Override
    public V remove(K key) {
        V oldValue = protectedSegment.remove(key);
        if (oldValue == null) {
            oldValue = probationarySegment.remove(key);
        }
        if (oldValue != null) {
            evict(key, oldValue);
        }
        return oldValue;
    }

