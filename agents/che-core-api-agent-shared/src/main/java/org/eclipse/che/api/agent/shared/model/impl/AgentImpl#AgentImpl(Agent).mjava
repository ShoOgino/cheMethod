  public AgentImpl(Agent agent) {
    this(
        agent.getId(),
        agent.getName(),
        agent.getVersion(),
        agent.getDescription(),
        agent.getDependencies(),
        agent.getProperties(),
        agent.getScript(),
        agent.getServers());
  }

